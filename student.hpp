#pragma once // prevents multiple definitions

#include <iostream> // print library
#include <string>

// Exercise 1: implementa la funciónque lleve a cabo
// Función que recibe  dos números y la opcion de 3 opciones de un menú:
// • S retornar la suma de los dos números
// • R retornar la resta de los dos números (el primero menos el segundo)
// • M retornar la multiplicación de los dos números
// • En caso de no recibir una opción válida, la función retornará -1
/
int operaNumeros(int a, int b, std::string opcion );


// ===============
// implementations

int operaNumeros(int a, int b, std::string opcion )
{
     return -1; // TODO: fix  
}
